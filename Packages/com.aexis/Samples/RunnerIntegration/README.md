# Runner Integration Sample

Applications retain their `MonoBehaviour` and model configuration outside the package while creating packaged sessions with `Aexis.Ncnn.NcnnInferenceSessionFactory.Create(ops)`.
