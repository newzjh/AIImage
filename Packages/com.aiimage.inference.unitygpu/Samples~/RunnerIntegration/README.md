# Runner Integration Sample

AIImage application runners retain their `MonoBehaviour` and model configuration in `Assets/Scripts`, while creating packaged sessions with `NcnnInferenceSessionFactory.Create(ops)`.
